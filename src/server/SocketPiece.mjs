class SocketPiece {
  constructor(type, left, top, direction, elements) {
    this.type = type;
    this.left = left;
    this.top = top;
    this.direction = direction;
    this.elements = elements;
  }
}

export default SocketPiece;
