const app = require("./app.mjs")
// add rooms with players

test("Test server side ", () => {

    // function to test
    // add a URL
    parseURL()

    // add a playername, roomnamme
    checkUserUnique()

    // add roomname, playername, socket
    addUserToRoom()
    
    })